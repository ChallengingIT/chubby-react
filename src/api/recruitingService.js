    // // src/api/recruitingService.js
    // import axios from 'axios';

    // const baseUrl = 'http://89.46.196.60:8443';

    // const apiClient = axios.create({
    // baseURL: baseUrl,
    // headers: {
    //     'Content-Type': 'application/json',
    // },
    // });

    // export const fetchCandidati = (params, headers) =>
    // apiClient.get('/staffing/react/mod', { params, headers });

    // export const deleteCandidato = (id, headers) =>
    // apiClient.delete(`/staffing/elimina/${id}`, { headers });

    // export const fetchTipologia = (headers) =>
    // apiClient.get('/aziende/react/tipologia', { headers });

    // export const fetchTipo = (headers) =>
    // apiClient.get('/staffing/react/tipo', { headers });

    // export const fetchStatoCandidato = (headers) =>
    // apiClient.get('/staffing/react/stato/candidato', { headers });
