    // import { BrowserRouter, StaticRouter } from 'react-router-dom';

    // const RouterComponent = ({ children, server, location }) => {
    // if (server) {
    //     // Usato per il rendering lato server
    //     return <StaticRouter location={location} context={{}}>{children}</StaticRouter>;
    // } else {
    //     // Usato per il rendering lato client
    //     return <BrowserRouter>{children}</BrowserRouter>;
    // }
    // };

    // export default RouterComponent;
